<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2>My Pragmatic Cart</h2>
<ul>    
  <% @cart.line_items.each do |item| %>
    <li><%= item.quantity %> &times;<%= item.product.title %></li>
  <% end %>
  <%= button_to 'Empty cart', @cart, method: :delete,
    confirm: 'Are you sure?' %>
</ul>
